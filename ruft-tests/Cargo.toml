[package]
name = "ruft-tests"
version = "0.0.1"
edition = "2018"
publish = false

[dev-dependencies]
ruft-server = { path = "../ruft-server" }
ruft-client = { path = "../ruft-client" }

tokio = { version = "1", features = ["rt"] }

portpicker = "0.1"
